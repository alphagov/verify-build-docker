pipeline {
   agent none
   stages {
       stage('Build and Push') {
           when { branch 'master' }
           agent any
           environment {
             DOCKER_HUB=credentials('dockerhub-govukverify')
           }
           steps {
             sh './build.sh'
           }
       }
   }
}
       